# Client configuration
# Option A: Direct WS URL (overrides host/port if set)
SERVER_WS_URL=
# Option B: Host and WS port (used if SERVER_WS_URL is empty)
SERVER_HOST=localhost
SERVER_WS_PORT=8081
# Subdomain to register
SUBDOMAIN=myapp
# Auth token must match the server
AUTH_TOKEN=change-me-strong-token
# Local target to forward to
# Option A: Full URL (overrides TO_HOST/TO_PORT/TO_PROTO if set)
TO=
# Option B: Build from parts if TO is empty
TO_HOST=localhost
TO_PORT=3000
TO_PROTO=http
